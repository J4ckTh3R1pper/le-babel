<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dayp308.chatroom.deprecated.mapper.ChannelMapper">
	<select id="getChannelList" resultType="Channel">
		SELECT * FROM channel WHERE server_id = #{server_id} ORDER BY channel_id
	</select>

	<select id="findChannelInServer" resultType="Channel">
		SELECT * FROM channel WHERE server_id = #{server_id} AND channel_id = #{channel_id}
	</select>

</mapper>
